import { glyphiconView } from 'legislator-lookup/helpers/glyphicon-view';
import { module, test } from 'qunit';

module('Unit | Helper | glyphicon view');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = glyphiconView([42]);
  assert.ok(result);
});
