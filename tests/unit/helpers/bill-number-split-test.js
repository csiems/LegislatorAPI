import { billNumberSplit } from 'legislator-lookup/helpers/bill-number-split';
import { module, test } from 'qunit';

module('Unit | Helper | bill number split');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = billNumberSplit([42]);
  assert.ok(result);
});
